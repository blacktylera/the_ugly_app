%d#filterrific_results
  %div
  %table(class="table table-hover")
    %tr
      %th Name
      %th Category
    - @spots.each do |spot|
      %tr
        / %h2= spot.id
        %td=link_to spot.name, spot_path(spot.id)
        %td= spot.category
= will_paginate spots
